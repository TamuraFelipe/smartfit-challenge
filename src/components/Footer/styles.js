import styled from 'styled-components';

export const Container = styled.footer`
    width: 100%;
    background: var(--dark-grey);
    padding-block: 10rem;

    img {
        display: block;
        width: 20rem;
        margin-inline: auto;
    }
    p {
        color: #fff;
        text-align: center;
    }
`;